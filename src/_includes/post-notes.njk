---
layout: base.njk
---

<article>
  <h1>{{ title }}</h1>
  <div class="post-meta">
    <time datetime="{{ date.toISOString().slice(0,10) }}">{{ date.toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric' }) }}</time>
  </div>
  <div class="post-content">
    {{ content | safe }}
  </div>
</article>

<div class="back-link">
  <a href="/">← Back to notes</a>
</div>